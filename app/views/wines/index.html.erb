<h1>Wines</h1>
<ol>
<% @wines.each do |wine|%>
	<li><%=link_to(wine.name,wine)%> created by <%=wine.winery %> and it is a <%=wine.varietal%></li>
<%end%>
<%= link_to 'New Wine', new_wine_path %>
</ol>
